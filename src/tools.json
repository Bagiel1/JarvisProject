[
    {
        "name": "list_notes",
        "description": "show all the names of the notes in the Obsidian graph written",
        "input_schema": {
            "type": "object",
            "properties": {
            },
            "required": []
        }
    },
    {
        "name": "read_note",
        "description": "show the content of a note",
        "input_schema": {
            "type": "object",
            "properties": {
                "name_file": {
                    "type": "string",
                    "description": "The exact name of the file to read, including the .md extension"
                }
            },
            "required": ["name_file"]
        }
    },
    {
        "name": "create_note",
        "description": "Create a note and a node in the graph of Obsidian",
        "input_schema": {
            "type": "object",
            "properties": {
                "name_file": {
                    "type": "string",
                    "description": "The name of the file, needs to make sense about what is talking in the note, put .md in the name"
                },
                "content_written": {
                    "type": "string",
                    "description": "The content about the note, if the note seems to reffers to another note you can connect them by putting two [, like [[jarvis]]"
                }
            },
            "required": ["name_file", "content_written"]
        }
    }
]